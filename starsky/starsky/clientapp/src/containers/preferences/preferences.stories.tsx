import { Preferences } from "./preferences";

export default {
  title: "containers/preferences"
};

export const Default = () => {
  return <Preferences />;
};

Default.story = {
  name: "default"
};
