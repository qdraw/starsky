steps:

  - task: Cache@2
    inputs:
      key: 'nuget | "$(Agent.OS)" | $(Build.SourcesDirectory)/starsky/nuget-packages-list.json'
      restoreKeys: |
        nuget | "$(Agent.OS)"
      path: ${{ parameters.nugetPackages }}
    displayName: Cache NuGet packages