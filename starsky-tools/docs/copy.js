var fs = require('fs');
var path = require('path');

function copyFileSync( source, target ) {

    var targetFile = target;

    //if target is a directory a new file with the same name will be created
    if ( fs.existsSync( target ) ) {
        if ( fs.lstatSync( target ).isDirectory() ) {
            targetFile = path.join( target, path.basename( source ) );
        }
    }

    fs.writeFileSync(targetFile, fs.readFileSync(source));
}

function copyFolderRecursiveSync( source, target ) {
    var files = [];

    //check if folder needs to be created or integrated
    var targetFolder = path.join( target, path.basename( source ) );
    console.log(targetFolder);
    if ( !fs.existsSync( targetFolder ) ) {
        fs.mkdirSync( targetFolder );
    }

    //copy
    if ( fs.lstatSync( source ).isDirectory() ) {
        files = fs.readdirSync( source );
        files.forEach( function ( file ) {
            if (file.indexOf("node_modules") === -1 && file.indexOf("package") === -1 &&
            file.indexOf(".md") === -1 && file.indexOf("docs.js") === -1 &&
            file.indexOf("server-develop.js") === -1 &&
            file.indexOf("copy.js") === -1
            ) {
              var curSource = path.join( source, file );
              if ( fs.lstatSync( curSource ).isDirectory() ) {
                  copyFolderRecursiveSync( curSource, targetFolder );
              } else {
                  copyFileSync( curSource, targetFolder );
              }
            }
        } );
    }
}

fs.writeFileSync(path.join("../","../", "docs", "readme.md"), '# Auto generated folder \n Open `index.html` to see the documentation. Do not edit this folder,\nDocs is generated by `starsky-tools/docs` and');
fs.writeFileSync(path.join("../","../", "docs", "__do_NOT_edit_this_folder__"), 'Open `index.html` to see the documentation. Do not edit this folder,\nDocs is generated by starsky-tools/docs');

copyFolderRecursiveSync(__dirname, path.join("../","../"))
